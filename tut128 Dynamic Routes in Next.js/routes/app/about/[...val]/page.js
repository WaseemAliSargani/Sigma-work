export default function Page({ params}) {
    // throw new Error("Error hai bhai")  //here you can create custom Erros and give then styles
    console.log(params)
    /// fetch your "blog"  post by its slug
    return <div> I am about page check console </div>
}